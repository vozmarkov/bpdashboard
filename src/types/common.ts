export interface IEmbDeal {
  total: number;
  order: string;
  created: string;
  is_verified: string;
  receipt: string;
  tracking: string;
  note: string;
  description: string;
  sku: string;
  price: number;
  quantity: number;
}
