import { createTheme } from "third-party";

// Create a Material-UI theme instance
// https://mui.com/customization/theming/
const theme = createTheme({
  typography: {
    fontWeightMedium: 600,
    fontSize: 17,
  },
});

export default theme;